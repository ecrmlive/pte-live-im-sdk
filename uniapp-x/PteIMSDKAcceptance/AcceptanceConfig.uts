import { PteIMBaseConfig, resolvePteIMBaseConfig } from '../../uni_modules/pte-im-sdk/utssdk/interface.uts'

/** Public test-server endpoints (SDK defaults). UserSig values are issued at runtime and never persisted. */
export const PteIMAcceptanceBaseConfig: PteIMBaseConfig = resolvePteIMBaseConfig({
  themeMode: 'system',
  language: 'en-US',
})
